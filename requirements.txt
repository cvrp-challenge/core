# Core Dependencies
numpy>=1.26.0
matplotlib>=3.5.0
pyyaml>=6.0

# VRP-specific libraries
vrplib>=1.4.0

# Progress bars and utilities
tqdm>=4.64.1

# Data analysis (optional but useful for benchmarking)
pandas>=1.5.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality
ruff>=0.1.0

# Jupyter for analysis (optional)
jupyter>=1.0.0
ipython>=8.6.0

# Tabular data display
tabulate>=0.9.0

# Note: First installation may take 3-10 minutes depending on your system.
# Use: pip install -r requirements.txt --progress-bar on
# Or:   pip install -r requirements.txt -v (for verbose output)

